# Source database (read-only access to OLTP)
SOURCE_DB_HOST=localhost
SOURCE_DB_PORT=5432
SOURCE_DB_NAME=clearsight
SOURCE_DB_SCHEMA=public
SOURCE_DB_USER=readonly_user
SOURCE_DB_PASSWORD=changeme

# Reporting database (read-write for reporting schema)
REPORTING_DB_HOST=localhost
REPORTING_DB_PORT=5432
REPORTING_DB_NAME=clearsight
REPORTING_DB_SCHEMA=rpt
REPORTING_DB_USER=reporting_user
REPORTING_DB_PASSWORD=changeme

# API configuration
API_HOST=0.0.0.0
API_PORT=8000
API_KEY=dev-api-key-change-in-production
API_WORKERS=4
API_LOG_LEVEL=info

# ETL configuration
ETL_BATCH_SIZE=5000
ETL_SCHEDULE_MINUTES=15
ETL_LOG_LEVEL=info

# Seed data (development only)
SEED_PROFILE=standard
SEED_RANDOM_SEED=42
